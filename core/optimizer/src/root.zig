const contract = @import("contract.zig");

pub const contract_version = contract.contract_version;
pub const package_version = contract.package_version;
pub const economics_status = contract.economics_status;
pub const Outcome = contract.Outcome;
pub const process = contract.process;
